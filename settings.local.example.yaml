modules:
  telegram:
    enabled: true
    bots:
      - name: main
        enabled: true
        bot_token: "1234567890:your-telegram-bot-token"
        workspace: data/your_workspace

# Put local LLM secrets in llm.local.yaml instead of settings.local.yaml.
