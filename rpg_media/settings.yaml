default_profile: local

base:
  providers:
    default_key: local_file
    local_file:
      enabled: true
      # 相对路径按仓库根目录解析；目录中的 PNG/JPEG/WebP 会作为 Demo 输出池。
      source_dir: data/media_provider
    # 首版仅保留能力目录，不执行网络调用。
    hosted_api:
      enabled: false
    comfyui:
      enabled: false

profiles:
  local: {}
  test: {}
  prod: {}
